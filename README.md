# 🔥 HotSpot -- Trending Aggregator

HotSpot 是一个基于 **Streamlit** 构建的轻量级实时热搜聚合器（Trending
Aggregator）。\
它支持从 **微博热搜榜** 与 **知乎热榜**
自动抓取最新热度数据，并以简洁、美观的 UI 展示。

------------------------------------------------------------------------

## 📸 Demo Screenshot

（如部署后可自行加入截图）

------------------------------------------------------------------------

## ✨ Features

-   **实时抓取微博热搜**（微博 API）
-   **实时抓取知乎热榜**（知乎 API）
-   **每 5 分钟自动刷新缓存**
-   **手动刷新功能**
-   **双列页面布局，简洁优雅**
-   **自定义 CSS 样式增强 UI**
-   **轻量、高性能、无服务器依赖**

------------------------------------------------------------------------

## 🛠️ Tech Stack

-   **Python 3.x**
-   **Streamlit**
-   **Requests**
-   **Pandas**
-   **Datetime**

------------------------------------------------------------------------

## 🚀 Getting Started

### 1. 克隆项目

``` bash
git clone https://github.com/your-repo/hotspot.git
cd hotspot
```

### 2. 安装依赖

``` bash
pip install -r requirements.txt
```

若无 requirements.txt，可使用以下依赖：

``` bash
pip install streamlit requests pandas
```

### 3. 运行应用

``` bash
streamlit run app.py
```

------------------------------------------------------------------------

## 📂 Project Structure

    hotspot/
    │── app.py           # 主应用逻辑
    │── requirements.txt # 依赖文件（可选）
    │── README.md        # 项目说明

------------------------------------------------------------------------

## 🔧 How It Works

### 📍 微博热搜获取

使用 `https://weibo.com/ajax/side/hotSearch` API，过滤广告项，解析
`note/word/num` 字段。

### 📍 知乎热榜获取

使用 `https://www.zhihu.com/api/v3/feed/topstory/hot-lists/total_heat`
API，从 `target.title` 等字段读取数据。

两个爬虫均使用： - 自定义 User-Agent\
- Streamlit `@st.cache_data(ttl=300)` 缓存 5 分钟

------------------------------------------------------------------------

## 🖥️ UI Layout

-   **双列布局**：左侧微博，右侧知乎\
-   **标题栏显示更新时间**\
-   **左侧 Sidebar 包含简介与一键刷新按钮**\
-   **自定义 CSS 支持加粗、高亮、hover 效果**

------------------------------------------------------------------------

## 🔄 Refresh / Caching

数据每 5 分钟自动刷新（缓存 TTL）。\
在侧栏点击 **"Refresh Now"** 可以强制清空缓存立即更新。

------------------------------------------------------------------------

## ⚠️ Notes

微博与知乎 API 均为未公开接口，可能存在： - 访问限制 / 出现 418/403\
- 页面结构调整导致解析失败

请在合理范围内使用。

------------------------------------------------------------------------

## 📜 License

MIT License --- Feel free to use, modify, distribute.
